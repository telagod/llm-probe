# Roadmap

LLM Probe 的演进路线图。标注 `[P0]` 为高优先级，`[P1]` 为中优先级，`[P2]` 为低优先级。

---

## Phase 1 — 多模型支持 `[P0]`

当前仅支持 Anthropic Messages API 兼容端点。目标：覆盖主流 LLM API 协议。

- [ ] **OpenAI Chat Completions API 适配器**
  - `/v1/chat/completions` 协议探针
  - function calling / tool_choice 语义映射
  - streaming SSE (`data: [DONE]`) 事件验证
- [ ] **Google Gemini API 适配器**
  - `generateContent` / `streamGenerateContent` 协议探针
- [ ] **统一 Probe 接口抽象**
  - 将 `internal/anthropic/` 抽象为 `internal/provider/` 接口
  - 各 provider 实现独立 adapter
  - CLI `-provider` 参数选择（auto-detect / anthropic / openai / gemini）
- [ ] **协议自动检测**
  - 根据 endpoint 响应头/错误格式自动识别 provider 类型

## Phase 2 — 检测能力增强 `[P0]`

- [ ] **模型指纹库**
  - 建立各模型的行为指纹基线（响应风格、token 分布、延迟特征）
  - 支持指纹比对判定模型真伪
- [ ] **计费欺诈检测**
  - 对比 usage 返回的 token 数与实际内容长度
  - 检测 token 虚报（多收费）
- [ ] **延迟指纹分析**
  - TTFT / TPS 分布特征
  - 与官方基线对比检测代理层额外延迟
- [ ] **多轮对话一致性**
  - 跨 turn 上下文保持度检测
  - 检测中间层是否截断/篡改 conversation history

## Phase 3 — Web 平台增强 `[P1]`

- [ ] **测试报告页面**
  - 可分享的公开报告链接
  - 报告对比视图（A vs B 端点）
- [ ] **端点监控仪表盘**
  - 定时巡检 + 历史趋势图
  - 异常告警（信任分下降、Hard-Gate 触发）
- [ ] **用户系统增强**
  - 自定义测试场景配置
  - 测试历史导出（JSON / CSV）
- [ ] **多语言 UI**
  - 英文界面支持
  - i18n 框架集成

## Phase 4 — 生态集成 `[P1]`

- [ ] **GitHub Action**
  - CI/CD 中自动检测 API 端点
  - PR check：信任分低于阈值自动 block
- [ ] **CLI 增强**
  - `llm-probe init` — 交互式配置生成
  - `llm-probe watch` — 持续监控模式
  - `llm-probe compare` — 双端点对比
- [ ] **Webhook / 告警集成**
  - Slack / Discord / 飞书通知
  - 自定义 webhook URL
- [ ] **SDK / Library**
  - Go SDK：嵌入到应用中做运行时检测
  - Python SDK：方便 ML 工程师使用

## Phase 5 — 社区与数据 `[P2]`

- [ ] **公开端点信誉库**
  - 社区贡献的 API 代理信誉评分
  - 匿名化检测数据聚合
- [ ] **Benchmark 排行榜**
  - 各端点信任分排名
  - 历史趋势公开展示
- [ ] **插件系统**
  - 自定义检测套件插件
  - 自定义评分规则插件

## Phase 6 — 企业特性 `[P2]`

- [ ] **多租户**
  - 团队/组织隔离
  - RBAC 权限控制
- [ ] **合规报告**
  - SOC 2 / ISO 27001 格式输出
  - 审计追溯链完整性证明
- [ ] **私有部署**
  - Helm Chart
  - Docker Compose 一键部署
  - 离线模式（无外部依赖）

---

## 版本规划

| 版本 | 里程碑 | 状态 |
|------|--------|------|
| v0.1 | Anthropic 协议全套件 + Web 控制台 + 信任评分 | ✅ 已完成 |
| v0.2 | OpenAI API 适配 + 模型指纹库 | 🔜 计划中 |
| v0.3 | Gemini 适配 + 计费欺诈检测 + 延迟指纹 | 📋 规划中 |
| v0.4 | 报告分享 + 端点监控 + GitHub Action | 📋 规划中 |
| v1.0 | 多 provider 稳定 + CLI 增强 + SDK | 📋 规划中 |

---

> 有想法或需求？欢迎 [提 Issue](https://github.com/telagod/llm-probe/issues) 讨论。
